<!--- TEMPLATENAME: internationalinclude.isml. This template is used for navigating to available regions for the current site--->
<isscript>
        response.addHttpHeader(dw.system.Response.X_FRAME_OPTIONS, dw.system.Response.X_FRAME_OPTIONS_SAMEORIGIN_VALUE);
</isscript>

<isinclude url="${URLUtils.url('Home-International','currentSite',session.privacy.currentSite)}" />