<!--- TEMPLATENAME: session_warn_popup.isml --->
<div class="session-warning">
	<h3>${Resource.msg('global.sessionexpired1','locale',null)}</h3>
	<p class="warning">${Resource.msg('global.sessionexpired2','locale',null)}</p>
	<button type="button" class="greybutton dialog-close session-warn">
		<a href="${URLUtils.url('Account-Show')}"><isprint value="${Resource.msg('global.login','locale',null)}" /></a>
	</button>
</div>
