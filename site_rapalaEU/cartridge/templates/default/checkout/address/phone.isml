<!--- TEMPLATENAME: phone.isml --->
<isinclude template="util/modules"/>

<isif condition="${pdict.countryValue === 'GB' || pdict.countryValue === 'IE'}">
	<isinputfield formfield="${pdict.fieldObject.formfield}" xhtmlclass="form-row-50 form-row-right"/>
</isif>
