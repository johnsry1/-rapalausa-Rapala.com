<!--- TEMPLATENAME: state.isml --->
<isinclude template="util/modules"/>

<isif condition="${pdict.countryValue === 'GB' || pdict.countryValue === 'US'}">
	<isinputfield formfield="${pdict.fieldObject.formfield}" type="${pdict.fieldObject.type}" required="${pdict.mandatoryAttribute}" />
</isif>
