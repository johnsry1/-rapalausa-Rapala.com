<iscomment>Print out product data for click events</iscomment>

<isscript>
	var tagManager = require('*/cartridge/scripts/googletags/Site_TagManager.js');
</isscript>

data-gtmdata='<isprint value="${JSON.stringify(tagManager.getProductData(pdict.gtm_pid))}" encoding="off"/>'