importPackage ( dw.catalog );

/* BM > Site > Products and Catalogs > Products */

var productID : String = "Enter_Product_ID_here";
var product : Product = ProductMgr.getProduct(productID);

return product;