importPackage ( dw.catalog );

/* BM > Site > Products and Catalogs > Catalogs */

var catalogID : String = "Enter_Catalog_ID_here";
var catalog : Catalog = CatalogMgr.getCatalog(catalogID);

return catalog;